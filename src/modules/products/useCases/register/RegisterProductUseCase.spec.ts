import { suite, test, expect } from "vitest";

suite("Register game use case", () => {
  test("should be able to sum a + b", () => {
    const a = 2;
    const b = 3;

    expect(a + b).toBe(5);
  });
});
